# Variables d'environnement nécessaires
# Copie ce fichier en .env et remplis les valeurs

# Port du serveur (Railway/Render le définit automatiquement)
PORT=5000

# URL publique du backend (sera fournie par Railway/Render après déploiement)
# Exemple: https://ton-app.railway.app ou https://ton-app.onrender.com
PUBLIC_URL=

# Secret JWT pour l'authentification (génère un secret aléatoire)
JWT_SECRET=ton-secret-jwt-super-securise-change-moi

# URL de la base de données PostgreSQL
# (Railway fournit DATABASE_URL automatiquement si tu ajoutes un plugin Postgres)
DATABASE_URL="postgresql://user:password@host:port/database"

# ============================================================================
# Stockage médias
# ============================================================================
# local  = stockage sur disque (dev uniquement)
# r2     = Cloudflare R2 (prod recommandé)
MEDIA_STORAGE="r2"

# Cloudflare R2 (S3-compatible)
R2_ENDPOINT="https://<accountid>.r2.cloudflarestorage.com"
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET=""
# URL publique du bucket (via domaine custom conseillé)
R2_PUBLIC_BASE_URL="https://cdn.example.com"

# ============================================================================
# Admin
# ============================================================================
# Clé one-shot pour créer/promouvoir un admin via /api/admin/bootstrap
ADMIN_BOOTSTRAP_KEY="change-me"

# ============================================================================
# Email - vérification email / mot de passe oublié
# ============================================================================
# Option 1 - Resend (recommandé, gratuit 3000/mois) : https://resend.com
# Crée une clé API sur resend.com/api-keys
# Domaine nox.world vérifié → utiliser noreply@nox.world
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM=Nox <noreply@nox.world>

# Option 2 - SMTP (Gmail, etc.)
# Gmail: active "App passwords" dans ton compte Google
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="tonemail@gmail.com"
# SMTP_PASS="mot-de-passe-app"
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Salt pour hasher les codes (optionnel mais recommandé)
AUTH_CODE_SALT="change-me"

# ============================================================================
# Stripe
# ============================================================================
# Clé secrète (serveur) - commence par sk_test_ / sk_live_
STRIPE_SECRET_KEY=
# Clé publique (mobile/web) - commence par pk_test_ / pk_live_
STRIPE_PUBLISHABLE_KEY=
# Webhook secret (Stripe CLI / Dashboard) - commence par whsec_
STRIPE_WEBHOOK_SECRET=

# ============================================================================
# Événements (booker)
# ============================================================================
# Nombre de jours minimum entre « aujourd'hui » et le jour de l'événement (création).
# 0 = désactiver la contrainte (tests locaux, démos avec événement le jour même).
EVENT_MIN_LEAD_DAYS=7

# Suppression d'événement côté booker alors qu'il existe déjà des tickets (tests / cleanup)
# true = autoriser (ne pas activer en prod). Les notes DJ/lieu liées à l'événement sont supprimées avant.
BOOKER_ALLOW_DELETE_WITH_TICKETS=false

# Scan billets — fenêtre « jour événement » :
# - Si défini : true / false tel quel.
# - Si non défini : sur Railway (RAILWAY_PUBLIC_DOMAIN / RAILWAY_ENVIRONMENT / RAILWAY_SERVICE_NAME) = true ; en local = false.
# Prod stricte : SCAN_TICKET_ALLOW_ANY_DAY=false dans le dashboard Railway.
# SCAN_TICKET_ALLOW_ANY_DAY=false

# Scan billets — mode test par app : même valeur que EXPO_PUBLIC_SCAN_TICKET_TEST_SECRET côté mobile (≥ 8 car.).
# Si défini, l'écran scan peut envoyer ce secret pour ignorer la contrainte « jour de l'événement » sans ouvrir tout le monde.
# SCAN_TICKET_TEST_SECRET=
