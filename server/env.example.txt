# Variables d'environnement nécessaires
# Copie ce fichier en .env et remplis les valeurs

# Port du serveur (Railway/Render le définit automatiquement)
PORT=5000

# URL publique du backend (sera fournie par Railway/Render après déploiement)
# Exemple: https://ton-app.railway.app ou https://ton-app.onrender.com
PUBLIC_URL=

# Secret JWT pour l'authentification (génère un secret aléatoire)
JWT_SECRET=ton-secret-jwt-super-securise-change-moi

# URL de la base de données SQLite (local) ou PostgreSQL (production)
# Pour Railway/Render, utilise leur base de données PostgreSQL fournie
DATABASE_URL="file:./dev.db"

# Exemple pour PostgreSQL (Railway/Render):
# DATABASE_URL="postgresql://user:password@host:port/database"

