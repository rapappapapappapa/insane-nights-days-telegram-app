# Expo / Metro charge les variables préfixées EXPO_PUBLIC_*
# Copie en `.env` à la racine de nox-mobile ou configure-les dans EAS Secrets.

EXPO_PUBLIC_API_BASE=https://ton-backend.railway.app

# Production stores : masquer le bandeau de test scan QR
# EXPO_PUBLIC_HIDE_SCAN_TEST_UI=true

# Google Sign-In — aligné sur les CLIENT_ID du serveur (GOOGLE_OAUTH_*).
# Build iOS : CLIENT_ID type « iOS »
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
# Android : CLIENT_ID type « Android » + empreinte SHA-1 dans Google Cloud
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=
# Client Web (souvent utilisé comme client commun / console Google)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
